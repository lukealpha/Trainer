#include <cstdio>
#include <iostream>
#include <cstring>
#include <algorithm>
#define MAXN 15
using namespace std;
int n, deg[MAXN];
int main(){
    scanf("%d",&n);
    double l, r;
    scanf("%lf%lf",&l,&r);
    for (int i = 0; i <= n; i ++){
        scanf("%d",&deg[i]);
    }
    
    return 0;
}